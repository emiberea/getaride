{% extends '::base.html.twig' %}

{% block content -%}
    <h1>Car creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('car') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
